# Product Traceability

This index records how supplied product references map to backend-owned capabilities. Approved decisions take precedence over source references; current backend structure follows [ARCHITECTURE.md](../../ARCHITECTURE.md) and Decision 0002, which supersede the original harness package design. Unresolved product differences remain explicit until the owning feature resolves them.

| Source | Owning capability | Backend responsibility | Consuming flow | Protecting test or planned marker | Decision or conflict |
| --- | --- | --- | --- | --- | --- |
| Mini specifications: student and administrator authentication | Identity | Validate Supabase JWTs and enforce ownership by the token subject | Mobile student flows; administrator web | `SecurityConfigurationTest`; student controller and service tests | Supabase Auth owns passwords; application tables store only identity links. See Decision 0001 |
| Use-case specifications: enrollment verification | Signup access | Require confirmation of an exact allowlisted school email domain | Supabase signup hook; mobile onboarding | `V4__restrict_signup_email_domain.sql`; PostgreSQL migration integration tests | Certificate submission and administrator approval were superseded by Decision 0001 |
| ERD revisions: student, administrator, and attachment identity modeling | Identity | Persist application profiles linked to Supabase user IDs | Mobile and administrator profile flows | `V3__align_identity_schema_with_supabase_auth.sql`; repository integration tests | Password hashes and student numbers were removed; signup verification attachments are not part of the current model |
| Mini specifications UC-S03/S04, use-case specifications, and ERD `COURSE` | Course catalog | Persist and expose active courses with stable identity, uppercase code, exact credit, broad category, and optional department | Grade entry/import, GPA simulation, graduation analysis, timetable, and future administrator course management | `V11__create_course_catalog.sql`; `CourseTest`; `CourseRepositoryIntegrationTest`; `CourseServiceTest`; `CourseControllerTest`; `OpenApiContractTest` | Required-course classification is not a course category; a future department/admission-year graduation-requirement relation owns it |
| Mini specifications UC-S03 and ERD `GRADE` | Student grade history | Persist authenticated student course attempts by academic year and term with credit and grade-point snapshots; expose owner-scoped registration, listing, correction, deletion, and explicit retake replacement APIs | Manual grade management and GPA summary now; OCR import and graduation analysis later | `V12__create_student_grade.sql`; `V13__link_retake_grade_attempt.sql`; `StudentGrade`; `StudentGradeRepositoryIntegrationTest`; `GradeServiceTest`; `GradeControllerTest`; `OpenApiContractTest` | RPL has no grade code or grade point. The course is immutable during correction, and manual deletion is hard deletion unless the attempt is referenced by a retake. The legacy `is_retake` column is preserved temporarily but is not API or calculation input |
| Mini specification 2.3 and use case UC-S03 | Retake-aware GPA summary | Calculate weighted overall and category GPA and completed credits from grade snapshots after excluding explicitly replaced attempts | Mobile academic-record summary; later simulation, graduation, and scholarship flows | `GradeSummaryCalculatorTest`; `GradeServiceTest`; `GradeControllerTest`; `OpenApiContractTest`; approved policy: `2026-09-20-grade-retake-gpa-policy-design.md` | RPL and `P` count as completed credit but not GPA; `NP` counts as neither; `F` counts in the GPA denominator but not completed credit. Simulation remains a separate feature |
| Mini specifications 2.7/3.1 and use case UC-S04 | Graduation-requirement lookup | Select an exact policy for every active major using department, admission year, and major type, then expose its active required courses through an authenticated API | Mobile requirement view and later graduation analysis | `GraduationRequirementServiceTest`; `GraduationRequirementControllerTest`; `OpenApiContractTest`; approved policy: `2026-09-21-graduation-requirement-read-service-design.md` | Results are ordered primary, double major, then minor. Missing policy data is an explicit error; completion and eligibility calculations remain deferred |
| Meal-recommendation references | Recommendation | Expose the finalized recommendation capability and vocabulary through the API | Mobile recommendation flow | Planned: domain and API tests | Meal naming differs across sources; resolve before endpoint and OpenAPI naming are finalized |
| Future Convergence College scope and institution-expansion references | Institution configuration | Use stable identifiers and an exact signup-domain allowlist | Mobile and administrator academic flows | `allowed_signup_email_domain`; department migration and repository tests | Phase one allows `inha.edu`; add future school domains explicitly without building speculative multi-tenant UI |

## Resolution rules

- Record a source, capability, consumer, test or planned-test marker, and unresolved decision for each new product requirement.
- Treat the backend OpenAPI document as the client contract after the corresponding API capability exists.
- Resolve a source conflict in an approved decision before implementing behavior that depends on it.
